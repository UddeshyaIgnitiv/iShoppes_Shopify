{% schema %}
{
  "name": "Special Offers",
  "tag": "section",
  "class": "special-offers-section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "SPECIAL OFFERS"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "IS Duty Free provides exclusive deals you won’t find anywhere else!"
    },
    {
      "type": "url",
      "id": "view_all_link",
      "label": "View All Link"
    }
  ],
  "blocks": [
    {
      "type": "offer",
      "name": "Offer",
      "settings": [
        {
          "type": "text",
          "id": "subtext",
          "label": "Top Label",
          "default": "LIMITED TIME OFFER"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Product Title",
          "default": "Product Promo Title"
        },
        {
          "type": "image_picker",
          "id": "banner_image",
          "label": "Product Image"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Product Link"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Special Offers",
      "category": "Custom",
      "blocks": []
    }
  ]
}
{% endschema %}

{% style %}
.special-offers-section {
  padding: 0 20px;
  background: linear-gradient(to right, #7e7d7e, #7e7d7e);
}

.special-offers-wrapper {
  display: flex;
  flex-wrap: wrap;
  gap: 40px;
  padding: 90px 50px;
  justify-content: center;
}

.special-offers-left {
  flex: 1 1 250px;
  max-width: 300px;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
}

.offer-section h2 {
  font-size: 28px;
  font-weight: 500;
}

.offer-section p {
  margin: 10px 0 5px 0;
}

.offer-section a {
  display: inline-block;
  font-weight: 500;
  color: #000;
  font-size: 18px;
  text-decoration: none;
}

.special-offers-right {
  flex: 3;
  display: flex;
  gap: 20px;
  overflow-x: auto;
  padding-bottom: 10px;
  justify-content: center;
}

.offer-card {
  background: #fff;
  border-radius: 16px;
  overflow: hidden;
  width: 430px;
  height: 462px;
  flex-shrink: 0;
  position: relative;
  box-shadow: 0 0 6px rgba(0,0,0,0.1);
}

.offer-card img {
  width: 100%;
  height: auto;
  display: block;
}

.offer-card .content {
  padding: 20px;
}

.offer-card .subtext {
  font-size: 12px;
  font-weight: 600;
  color: #fff;
  text-transform: uppercase;
}

.offer-card .title {
  font-size: 22px;
  font-weight: 600;
  margin-top: 5px;
  color: #fff;
  line-height: 1.2;
  max-width: 90%;
}

.offer-card .arrow {
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 50px;
  background-color: rgba(0,0,0,0.8);
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 20px;
}

/* Responsive */
@media screen and (max-width: 768px) {
  .special-offers-wrapper {
    flex-direction: column;
    padding: 30px 20px;
    gap: 10px;
  }
  .special-offers-left {
    max-width: 100%;
  }
  .special-offers-right {
    flex-wrap: nowrap;
    overflow-x: auto;
    justify-content: flex-start;
    max-width: 100%;
  }
  .offer-card {
    width: 100%;
    min-width: 250px;
  }
  .special-offers-section {
    padding:0px;
  }
}
@media screen and (max-width: 800px) {
  .special-offers-right {
    flex: unset;
    justify-content: start;
  }
}
{% endstyle %}

<section class="special-offers-section">
  <div class="page-width special-offers-wrapper">
    <div class="special-offers-left">
      <div class="offer-section">
        <h2>{{ section.settings.title }}</h2>
        <p>{{ section.settings.description }}</p>
        {% if section.settings.view_all_link %}
          <a href="{{ section.settings.view_all_link }}">View All →</a>
        {% endif %}
      </div>
    </div>
    <div class="special-offers-right">
      {% for block in section.blocks %}
        <a href="{{ block.settings.link }}" class="offer-card" {{ block.shopify_attributes }} style="background: linear-gradient(to bottom, rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0) 60%), url({{ block.settings.banner_image | image_url: width: 600 }}); background-size: cover; background-position: center; background-repeat: no-repeat;">
          <div class="content">
            <div class="subtext">{{ block.settings.subtext }}</div>
            <div class="title">{{ block.settings.title }}</div>
          </div>
          <div class="arrow">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M5 12H19" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M12 5L19 12L12 19" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
        </a>
      {% endfor %}
    </div>
  </div>
</section>
